{
  "name": "stuyshuttle-push",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "keys": "node gen-vapid.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
